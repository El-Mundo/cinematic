# Run these commands in Mac Terminal to see the images in PixPlot
# Must run with numpy<1.24.0

cd # CD to the directory "CV/PixPlot/Temp/" under this project

conda activate pixplot

pixplot --image "images/*.png"

python -m http.server 2333

# Then the results can be viewed at http://localhost:2333/output